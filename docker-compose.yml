# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    env_file:
      - database.env
    volumes:
      - /var/lib/postgresql/data/
    ports:
      - 5432:5432
